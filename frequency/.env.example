# Frontend (Vite) — Render API base URL, e.g. https://frequency-api.onrender.com
VITE_API_URL=

# Same OAuth client id configured on the backend (GOOGLE_CLIENT_ID). Leave
# blank to keep the "continue with Google" button hidden.
VITE_GOOGLE_CLIENT_ID=
